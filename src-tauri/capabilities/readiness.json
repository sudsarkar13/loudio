{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "readiness",
	"description": "Capabilities for the System Readiness window. Narrower than the main window's: readiness reports on dependencies and applies updates, so it needs no clipboard and no compact-mode window geometry controls. It does need the updater and a restart, which is the one thing only this window does.",
	"windows": [
		"readiness"
	],
	"permissions": [
		"core:default",
		"core:window:allow-close",
		"core:window:allow-show",
		"core:window:allow-set-focus",
		"core:window:allow-minimize",
		"core:window:allow-unminimize",
		"core:window:allow-start-dragging",
		"core:app:allow-default-window-icon",
		"updater:default",
		"process:allow-restart"
	]
}
