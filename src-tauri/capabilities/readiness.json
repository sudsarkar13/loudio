{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "readiness",
	"description": "Capabilities for the System Readiness window. Deliberately narrower than the main window's: readiness installs dependencies and reports on them, so it needs no clipboard, no filesystem writes and no compact-mode window geometry controls.",
	"windows": [
		"readiness"
	],
	"permissions": [
		"core:default",
		"core:window:allow-close",
		"core:window:allow-show",
		"core:window:allow-set-focus",
		"core:window:allow-minimize",
		"core:window:allow-unminimize",
		"core:window:allow-start-dragging",
		"core:app:allow-default-window-icon"
	]
}
